<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.iseungcheon.wanhea">

    <!-- 네트워크 사용에 대한 퍼미션 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".base.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Customize.Customize"
            android:label="@string/title_activity_customize" />
        <activity android:name=".DB_Check" />
        <activity android:name=".base.RetrofitActivity" />
        <!--<activity-->
            <!--android:name=".HomeActivity"-->
            <!--android:label="@string/title_activity_home"-->
            <!--android:theme="@style/AppTheme" />-->
        <activity
            android:name=".ItemActivity"
            android:label="@string/title_activity_item"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ItemInforActivity"
            android:label="@string/title_activity_item_infor"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>